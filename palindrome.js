function check_palindrome(str_Palindrome)
var str = string_enter.localLowerCase().replace(palindrome)

var cCount = o;
if (cString === ""){
    return false
    console.log("palindrome")
}
return false;
if  ((cStr.length) % 2 === 0){
count = cString.length 
}
